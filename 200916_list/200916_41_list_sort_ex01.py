fruit = ['apple', 'mango', 'kiwi', 'orange']
test  = ['A', 'z', 'a', 'Z', 'c'] 

if 0 :  # 실행을 위해서는 0을 1로 수정합니다.
    # fruit에 대해 오름차순으로 정렬합니다. (sample)
    fruit.sort()
    print(fruit)
    # fruit에 대해 내림차순으로 정렬합니다


    print(fruit)

if 0:
    # alphabet에 대해 오름차순(UNICODE순)으로 정렬하여, 
    # 결과를 r1에 바인딩합니다.
    

    print(f'alphabet = {alphabet}\nr1 = {r1}')
